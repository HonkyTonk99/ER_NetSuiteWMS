/**
 * SuiteCloud CLI project configuration.
 * The SDF project lives under `src/` (manifest.xml, deploy.xml, FileCabinet, Objects).
 * Deploy/validate commands target an environment by --authid — see DEPLOY.md.
 */
module.exports = {
    defaultProjectFolder: 'src',
};
