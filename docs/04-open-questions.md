# 04 — Open Questions / Decision Register

Owner and date to be filled in during `T-0.3`. **Blockers must be decided before their dependent
phase is scheduled** — they are not "we'll work it out in the sprint" questions.

> **Updated 2026-08-08 per rulings D-08 (serial out of scope), D-09 (inbound via WMS) and D-10 (WMS
> primary, NetSuite retains commitment and cost authority).**
>
> **Closed:** Q-03, Q-04, Q-14 (D-03) · Q-18, Q-19, Q-20 (D-07) · Q-21, Q-22, Q-23 (D-08) ·
> Q-05 receiving portion and Q-15 (D-09 — expiry is now captured at receipt).
> **New:** Q-24 costing method, Q-25 manufacturing transaction type, Q-26 negative inventory
> setting, Q-27 over-receipt tolerance.
>
> **Updated 2026-08-08 per D-11** — costing is NetSuite's concern and out of WMS scope; negative
> inventory is permitted in the WMS and forbidden in NetSuite. **Closes Q-24 and Q-26.**
>
> **Updated 2026-08-09 — recorded as decisions D-12…D-19 (see `05-decisions-log.md`).** Every ruling
> below now has a D-number and the log is current for the first time since D-11.
> - **D-12** *(corrected)* — custom fields aren't unique but **`externalid` is** → AD-04 uses
>   `externalid`=UUID as primary guard **plus** committer dedupe as safety net; AD-05 withdrawn **by
>   choice** (a lock is possible on `externalid` but rejected on simplicity), lock record deleted.
>   **Propagated across the spec.**
> - **D-13** — Q-01: handheld = responsive PWA, Android-first, served from NetSuite; iOS out of scope.
>   AD-09's "PWA cannot deliver persistence" claim corrected; residual risk accepted in writing.
> - **D-14** — Q-08: multi-location in scope; location is mandatory session context; bin names
>   location-prefixed; switching location requires connectivity.
> - **D-15** — delivery is an Account Customization Project, not a SuiteApp.
> - **D-16** — Q-16: seven bin types + `availableForFulfilment`; surfaces F-26 → Q-29.
> - **D-17** — T-0.4 case (b): bin data migrates from a third-party app.
> - **D-18** — Q-13: NetSuite WMS SuiteApp not installed; D-07 confirmed.
> - **D-19 CONFIRMED 2026-08-09** — one Available-Without-Login Suitelet (GET serves the SPA, POST is the
>   JSON API), 1,000 units/request, dedicated Execute-As role. Closes **Q-02**, resolves **Q-30**.
>   Consequences: **D-21** (device auth mandatory), **F-29** (concurrency throttling), **AD-19**
>   (privilege separation), `allowed_locations` now WMS-enforced. New finding **F-27** (exposed endpoint).
> - **D-22** — Transfer Order **outbound in scope** (resolves **Q-33** as option a). New finding **F-30**
>   (TO ordering exception to invariant #18). Surfaces **Q-36** (in-transit location) and **Q-37**
>   (intercompany, assumed out).
> - **Q-34 answered** as an assumption (WMS has the concurrency pool to itself — verify pre-go-live).
>   **Q-35** new (File Cabinet cache-warm — unverified proposal). Also closed earlier: **Q-10** (moot).
> - **D-14 propagated** (prior pass). Open: **Q-29** (F-26 — with sponsor; drives the location class
>   with Q-36), **Q-31** (one-location-per-session, assumed), **Q-32** (order spanning locations).
>
> **No hard blocker remains for a phase *start*.** Q-06, Q-07, Q-09, Q-11, Q-12, Q-17, Q-25, Q-27, Q-29,
> Q-31, Q-32, Q-35, Q-36, Q-37 are needed *during* their phases; Q-05 residual (cycle counting /
> structured RMA) is a release-scope call. **Phase 1 gates on T-0.3 closing this register with owners +
> dates.** The **location class** (Q-29 + Q-36) is now *likely required* — held until both land, built once.
> D-13 (PWA) detailed design remains held pending the three D-19 developer confirmations.
>
> **Updated 2026-08-10 — the register is now organised by owner into three sheets, and D-23 carves three
> pure-logic tasks out of the Phase-1 gate.**
> - **D-23** — **T-6.1, T-2.3b, T-2.6 are buildable now**, before this register closes, under a
>   CI-enforced no-`N/`-import boundary. They import no SuiteScript, touch no record, and no open
>   question changes their inputs or outputs. **These three only; a fourth needs a new ruling.** Every
>   other task stays held on T-0.3.
> - **Sheet A — NetSuite developer** (platform facts, **no safe default — stay held until answered**,
>   chase date **2026-08-17**): **Q-35** (A7 File-Cabinet cache-warm), **Q-40** (A1–A3 TO ledger-adapter
>   transforms + receipt ordering), **Q-41** (A4 Suitelet non-200 status), **Q-42** (A5 governance cost
>   per create/submitFields), **Q-43** (A6 create-without-edit permission), **Q-44** (A8 Map/Reduce
>   concurrency pool).
> - **Sheet B — sponsor** (each has a recommendation that becomes a **recorded assumption** if
>   unanswered by the review date **2026-08-24**): **Q-31** (B5 one location per session — assumed yes),
>   **Q-32** (B6 order spanning locations → refuse + exception, opt a), **Q-37** (B4 intercompany TOs →
>   out of scope v1). **No safe default, stay open:** **Q-45** (B7 customer returns receipting), **Q-46**
>   (B8 cycle counting), **Q-48** (camera scanning — a dependency of D-24 phone support).
> - **New decisions this pass:** **D-24** — the PWA is **responsive across desktop / tablet / phone with
>   the same operations** (a sponsor requirement, formalised after a gap-closure pass had silently
>   narrowed it to one rugged-Android device); **D-25** — a version-mismatched client **may always drain
>   its queue**, only *new work* is gated (fail-closed on ingest would strand real stock movements).
> - **The Q-29 / Q-36 / location-class knot (must fire together or not at all).** **Q-29** (B1
>   non-fulfillable stock) recommends opt (a) — a **separate NetSuite holding location**. But applying
>   (a) *creates* the very holding location the **location class** (B2, D-14) exists to exclude from the
>   picker, waves and cache warm — and the location class is blocked on **Q-36** (B3 in-transit), which
>   has **no default**. So **Q-29's fallback is CONDITIONAL on Q-36**: firing (a) alone would leave a
>   holding location in the operator picker with nothing to exclude it — an unhandled state. **Q-36 is
>   therefore escalated to effectively required by 2026-08-24** and chased on **both** Sheet A (in-transit
>   is a platform/config fact) and Sheet B (an operations fact). **If Q-36 is still open on the date,
>   Q-29 and the location class BOTH stay open and the sponsor is told why — the system does not default
>   into the unhandled state.**
> - **Sheet C — you / Todd** (account & data facts nobody else can get; **the hard Phase-1 gate**):
>   **T-0.1** item census (PLAIN vs LOT share, any serialised items, namespace collisions), **T-0.4**
>   migration source + owner, **T-0.3** register closure itself, **Q-47** (environment — sandbox, SDF
>   deploy rights, refresh schedule).
>
> **Updated 2026-08-11 — platform-facts pass (D-27, see `08-platform-facts.md`).** Verified NetSuite
> behaviour is now recorded canonically; several facts are **SANDBOX-PENDING** and are discharged by the
> new **T-0.8** (whose **T1** gates all of Phase 1). Register effects:
> - **Q-35 CLOSED (D-27)** — File Cabinet Available-Without-Login cache warm adopted as the *required*
>   design (zero integration concurrency, PF-05).
> - **New Sheet C rows:** **Q-49** (service tier + SuiteCloud Plus count — sizes all of F-29), **Q-50**
>   (Enhanced Validations SuiteApp / Prevent Negative Inventory), **Q-51** (Multi-Book), **Q-52**
>   (OneWorld cross-subsidiary). These are account facts, read from the sandbox.
> - **New Sheet B row:** **Q-53** (GL write-off offset account + finance-approval threshold).
> - **New Phase-0 task T-0.8 (sandbox verification):** **T1** (Available-Without-Login Suitelet with
>   Execute-As role creates a custom record when called logged out — **gates all of Phase 1**), **T4**
>   (the `Indexed` field setting + its SDF representation), **T6** (the preference identifiers of PF-28).
>
> **Updated 2026-08-11 — serial reissue (D-29…D-33).** A pass lost in relay is reissued: **serial is IN
> scope (D-29 supersedes D-08).** Register effects:
> - **Q-45 CLOSED (D-32)** — customer returns (RMA) in scope. **Q-32 CLOSED (D-30)** — the multi-location
>   refusal recommendation is withdrawn; waving keys on the line's location.
> - **Q-21 RE-OPENED, Q-22/Q-23 RESTATED (D-29)** — serial scan volume (F-21) and serial bin rules
>   (invariants #21/#22) are live again.
> - **Q-53 now also covers the D-33 write-off** offset GL account + approval threshold. **Q-37**
>   (intercompany TOs out of scope) still open — confirm.
> - **New tasks:** T-5.10 (RQD isolation), T-5.11 (RMA receipt), T-5.12 (write-off), T-2.6c (serial
>   handler — carved-out, own pass), **T-4.8** (tracked: fold the D-27 committer rulings into the T-4.x
>   bodies).

| ID | Question | Why it matters | Blocks | Recommendation | Owner | Decision |
|---|---|---|---|---|---|---|
| ~~Q-01~~ | **CLOSED 2026-08-09 (D-13).** Handheld is a **responsive PWA, Android-first, served from NetSuite; iOS out of scope.** ⚠️ *This overrides the plan's recommendation of a native app and runs against AD-09's stated position that a browser/PWA client cannot dependably deliver durable offline persistence and background sync on rugged Android. **Propagated 2026-08-10** — AD-09 corrected (D-13); T-3.2 (durable queue), T-3.4 (client) and T-12.5 (tests) now specify the PWA delivery explicitly (IndexedDB cache + queue, `navigator.storage.persist()`, service-worker shell-only), with the residual offline-durability risk (no background sync un-foregrounded; eviction if `persist()` denied) accepted in writing per D-13.* | Phase 3 | *(superseded by the ruling above)* | TK | **Closed** |
| ~~Q-02~~ | **CLOSED 2026-08-09 (D-19) — subsumed into T-3.3.** Auth is **Option C**: Available-Without-Login Suitelet, operator ID + hashed PIN, HMAC session token; **no TBA in the browser, no per-operator NetSuite user.** Not a device-credential question anymore. See F-27 (exposed endpoint) and T-3.3. | — | — | — | TK | **Closed** |
| ~~Q-03~~ | **CLOSED 2026-08-07 (D-03).** No bulk → picking consumes available bin batches directly, FEFO; when exhausted the SKU is out of stock. No blocked task, no escalation. | — | — | — | TK | **Closed** |
| ~~Q-04~~ | **CLOSED 2026-08-07 (D-03).** One bin = one batch, so demand spanning bins spans batches. Summary picking is one task per SKU **per bin**. FEFO across bins; orders may split across batches. | — | — | — | TK | **Closed** |
| **Q-05** | *(Reduced by D-09; split 2026-08-10.)* **Receiving and putaway are now IN scope** (Phase 5B). The two residuals are now first-class sponsor questions: **returns/RMA putaway → Q-45 (B7)** and **cycle counting → Q-46 (B8)**. | `COUNT` sits in the event enum with no specification; returns have lot and bin implications. Both are smaller now that the inbound framework exists. | Release scope | **Split** — see Q-45 and Q-46 for owned decisions. | Sponsor | **Split → Q-45, Q-46** |
| **Q-06** | Packing label and packing slip: which printer/label platform, and what routing by pack station? | §2.4 says packing "triggers packing label generation" with no further specification. Despatch cannot operate without it. | T-7.5 | Confirm the incumbent label platform before designing anything new. | | |
| **Q-07** | Is UOM conversion in scope? The cache spec holds base UOM and conversion factors but nothing in any flow uses them. | If orders are in cases and pickers scan eaches, every quantity in the system needs conversion — a pervasive change, not a local one. | Phase 2 & 4 | Confirm early. If eaches-only, remove UOM from the cache spec to avoid implying capability that does not exist. | | |
| ~~Q-08~~ | **CLOSED 2026-08-09 (D-14).** **Multi-location in scope.** Location is **mandatory session context**; bin names are **prefixed with the location code**; the handheld cache **warms on location selection**, so **switching location requires connectivity** (a bounded exception to offline-first — you work offline *within* a location). ⚠️ *Propagation of location scoping across the data model, wave/zone model and handheld login is still to do — flagged in D-14.* | — | — | — | TK | **Closed** |
| **Q-09** | Scan event retention: is 7 days acceptable, and what is the archive destination? What are the audit and regulatory retention obligations (lot traceability, food/pharma)? | The FRD says "purges or archives" — those are very different things. Purging destroys the only record of who scanned what. | T-11.1 | Archive to an external store before purge. Confirm lot-traceability retention obligations with compliance. | | |
| ~~Q-10~~ | **CLOSED 2026-08-09 — moot.** WMS bin state tracks **physical** quantity only and never sees NetSuite's available/committed split. A bin holding committed stock is **occupied**, because the stock is physically there. There is no reserved-but-not-available dimension in the WMS to test against. | — | — | — | TK | **Closed** |
| **Q-11** | What is the partial-fulfilment policy on an unresolvable short pick — ship partial, hold the order, or backorder? | Drives T-7.4 behaviour and has direct customer-experience consequences. | T-7.4 | Confirm with customer service; likely varies by customer or order type. | | |
| **Q-12** | Cart/tote capacity — how many order positions does a pick cart hold? | Hard cap on wave cluster size (AD-10). Without it, clustering can produce a wave no picker can physically carry. | Phase 6 | Measure the actual carts. | | |
| ~~Q-13~~ | **CLOSED 2026-08-09 (D-18).** The Oracle NetSuite WMS SuiteApp is **not installed** in the target account. It would have required Bin Management enabled, contradicting D-07 — so this confirms **D-07 is settled, not provisional.** T-0.1 retains a general namespace-collision check on its own merits (ACP hygiene). | — | — | — | TK | **Closed** |
| ~~Q-14~~ | **CLOSED 2026-08-07 (D-03).** Direct picking from non-pick-face bins is permitted. Allocation prefers the UNIT pick face, then falls through to any bin holding the SKU. | — | — | — | TK | **Closed** |
| ~~Q-15~~ | **CLOSED 2026-08-08 (D-09).** Lot expiry is captured at receipt on the handheld (T-5.5), so FEFO data is created correctly at source rather than needing a back-fill. Existing stock still needs a one-off check during Phase 10 remediation. | — | — | — | TK | **Closed** |
| ~~Q-18~~ | **CLOSED 2026-08-08 (D-07).** Lot recall traceability: LOT items post lot detail to NetSuite normally, so standard lot traceability is intact wherever items are lot-numbered. The exposure was a consequence of the tier model, which no longer exists. *(Traceability of **which bin** remains WMS-only — but that was never a regulatory question.)* | — | — | — | TK | **Closed** |
| ~~Q-19~~ | **CLOSED 2026-08-08 (D-07).** Advanced Bin / Numbered Inventory Management is **not required** — no NetSuite bins. No licence decision, no cost. | — | — | — | TK | **Closed** |
| ~~Q-20~~ | **CLOSED 2026-08-08 (D-07).** Lot costing works normally for lot-numbered items; the constraint was tier-derived. | — | — | — | TK | **Closed** |
| ~~Q-21~~ | **RE-OPENED by D-29, then RESOLVED by D-34 (2026-08-11).** Serial share was closed by D-08, re-opened by D-29 (F-21 live again). **D-34 resolves it as a *parameter*, not a measured gate:** the design must function at **up to 100% serialised**, so T-0.1's census *tunes configuration* (cache size, scan-volume model, batch size) rather than gating the design. | — | — | — | Todd | **Resolved by D-34 (parameterised)** |
| ~~Q-22~~ | **RE-OPENED by D-29, RESOLVED by restatement (2026-08-11).** The serial bin rule is now **invariants #21/#22** (a serial is in exactly one bin; bin-state quantity = count of serial rows). Bin state stays scalar; per-unit truth is `customrecord_wms_serial_state` (§3.13). | — | — | — | Todd | **Resolved (D-29 — invariants #21/#22)** |
| ~~Q-23~~ | **RE-OPENED by D-29, RESOLVED by restatement (2026-08-11).** Serials are **captured at entry** (WO completion, PO receipt, RMA receipt) and referenced everywhere else — directed serial picking is in scope (Part D flows). | — | — | — | Todd | **Resolved (D-29 — Part D flows)** |
| ~~Q-16~~ | **CLOSED 2026-08-09 (D-16).** Bin types are **UNIT, BULK, STAGE, RECEIVING, QUALITY, RETURN, DEFECT.** New policy attribute **`availableForFulfilment`** — TRUE for UNIT and BULK only; stock in any other type is physically present but must be moved into UNIT/BULK before it can be picked. Enforced in T-7.1 (allocation), T-5.2 (replenishment), T-5.4 (putaway); **not** filtered from reconciliation (T-8.3). Surfaces new finding **F-26** (NetSuite over-commits non-fulfillable stock). Seeds §3.9b policy table and types the migrated bins (T-0.4 case b, Phase 10). | — | — | — | TK | **Closed** |
| **Q-17** | *(New, from D-04.)* What is the maximum acceptable **offline duration** for a handheld before the operator is blocked? | Sets the local cache staleness limit and the reconnect conflict window (T-3.2, T-3.5). Too short blocks pickers in dead spots; too long lets conflicts accumulate. | Phase 3 | Start at 30 minutes warn / 60 minutes block, then tune from measured Wi-Fi coverage. | | |
| ~~Q-24~~ | **CLOSED 2026-08-08 (D-11).** NetSuite runs costing; the WMS supplies quantity, date and lot and designs nothing around valuation. No per-item event ordering is required for costing. | — | — | — | TK | **Closed** |
| **Q-25** | *(New, from D-09.)* For Work Order output, does the account use **Work Order Completion** or **Assembly Build**? | Different transaction types with different subrecord shapes. T-5.7 needs to target the right one. | Phase 5B | Confirm the manufacturing configuration during T-0.1. | | |
| ~~Q-26~~ | **CLOSED 2026-08-08 (D-11).** **The WMS may go negative; NetSuite may not.** Outbound never posts where NetSuite lacks quantity — it defers and retries (T-4.7). Negative bin state is permitted but diagnostic (F-25). | — | — | — | TK | **Closed** |
| **Q-27** | *(New, from D-09.)* What **over-receipt tolerance** applies against a PO line, and who may approve beyond it? | T-5.5 blocks or accepts based on this. Warehouses routinely receive slightly more than ordered. | Phase 5B | Confirm with procurement; a percentage tolerance with supervisor override above it is typical. | | |
| **Q-29** | *(New, from F-26 / D-16.)* How should the system prevent NetSuite **over-committing stock held in non-fulfillable bins** (QUALITY/RETURN/DEFECT)? NetSuite counts it toward quantity on hand and commits it; the WMS cannot pick it, so those orders short-pick. Mirror of F-22. | Breaks the AD-17 commitment contract from the NetSuite side; NetSuite availability becomes fiction by the quarantine volume. | Phase 6/7 (allocation), Phase 1 if (a) chosen | **(a) RECOMMENDED** — a separate NetSuite **location** for non-fulfillable stock (moves across the boundary post an Inventory Transfer); (b) same location, accept permanent over-commit + short-pick handling; (c) NetSuite inventory status (Advanced Inventory, likely unavailable). Cross-refs Q-08 (multi-location). **If (a): the holding location is not a warehouse — D-14 then needs a location _class_ (operational vs holding) so it is excluded from the operator picker, wave generation and cache warm. See D-14 (→ Q-29).** **Status: with sponsor.** | Sponsor | **Open — assumption-if-no-response: opt (a), review 2026-08-24 — but CONDITIONAL on Q-36. Opt (a) creates a holding location; the location class that excludes it from the picker/waves/warm is blocked on Q-36 (no default). So Q-29's default MUST NOT fire unless Q-36 is also answered — else a holding location sits in the picker with nothing to exclude it. If Q-36 is still open on 24 Aug, BOTH stay open and the sponsor is told why, rather than defaulting into an unhandled state.** |
| ~~Q-30~~ | **RESOLVED 2026-08-09 (D-19).** Option C (Available-Without-Login Suitelet + operator identity in payload) means **no NetSuite user per operator** — the licence cost is avoided. Audit attribution is preserved by validating the operator against `customrecord_wms_operator` and writing `custrecord_se_operator`. The residual is the exposed endpoint — F-27, mitigated by T-3.3 + device auth (D-21). **D-19 CONFIRMED 2026-08-09.** | — | — | — | TK | **Closed** |
| **Q-31** | *(New, from D-14 propagation.)* **Assumption to confirm:** one operator holds **one location per session** — switching is an explicit action (full cache purge + re-warm, needs connectivity), never two locations concurrently. | Governs the login/session model, cache-warm cost and the T-3.2/T-3.4 flow. If multi-location sessions were ever allowed, the cache and scan-stamping model would change materially. | Phase 3 | **Assumed yes (one at a time).** Recorded as an assumption per D-14; confirm before Phase 3 build. | Sponsor | **Open — assumption stands: one location per session, review 2026-08-24** |
| ~~Q-32~~ | **RESOLVED 2026-08-11 (D-30, Part G) — the refusal recommendation is WITHDRAWN.** A sales order whose lines carry different locations is a **supported NetSuite configuration (PF-18), not an error.** Replacement rule: **allocation and waving key on the LINE's location (`line.inventorylocation \|\| line.location`, PF-34); one order generates work in each location; each location's lines wave independently; the order completes when both are picked.** D-14's "a wave never spans locations" stands — a wave is one operator's physical run at one site — but that is a property of waves, not a constraint on orders. | — | — | — | Sponsor | **Closed (D-30) — refusal withdrawn** |
| ~~Q-33~~ | **RESOLVED 2026-08-09 (D-22) — option (a).** Transfer Order outbound **is in scope**: the WMS picks/stages/ships TOs out of the source location. Wave/allocation/pick/stage extend to transaction type `transferorder` (same engine, not a fork); the ledger adapter gains the TO-fulfilment shape (transform target pending developer confirmation). Bin state stays accurate. Ordering exception F-30; in-transit location Q-36. | — | — | — | TK | **Closed** |
| ~~Q-34~~ | **ANSWERED 2026-08-09 — as a standing assumption with an expiry (Todd).** No other systems consume Integration Governance concurrency; the WMS **has the pool to itself.** ⚠️ **Not a verified constant** — it becomes false the day another integration is added. **Pre-go-live: verify the account's actual limit and current consumers; runbook: any new integration requires re-sizing.** Does **not** relax F-29 — a larger pool raises the ceiling, not the burst behaviour. | Sizes F-29's batch bound and slot allocation (T-0.2). | Pre-go-live verify | Standing assumption; verify before go-live. | Todd | **Assumed** |
| ~~Q-35~~ | **RESOLVED 2026-08-11 (D-27) — affirmatively.** A File Cabinet file marked Available Without Login is served **without script execution and consumes ZERO integration concurrency** (PF-05). The per-location cache-warm payload **is** published as a File Cabinet file, regenerated by a scheduled script, downloaded by the device — **adopted as the REQUIRED design**, not optional: at the F-29 pool sizes (PF-01) the shift-start burst is otherwise unsurvivable. Versioning via **Generate URL Time Stamp** (cache-buster), cross-ref D-13 app-update path. | — | — | — | TK | **Closed (D-27)** |
| **Q-36** | *(New, developer/sponsor.)* Does the client use **in-transit inventory** on transfer orders, or **direct ship-and-receive**? | If in-transit, an intermediate NetSuite location exists — and it is **not a warehouse**: it must be excluded from the operator picker, wave generation and cache warm (the **location class**, same as Q-29). | Phase 5B/6 | Confirm. Drives whether the location class (D-14) is required. | Developer / Sponsor | **Open — ESCALATED: effectively required by 2026-08-24. No safe default (fact about operations), but Q-29's fallback cannot fire without it (a holding location with nothing to exclude it is an unhandled state). Answer it on the same date as the Sheet-B batch — chase on BOTH Sheet A (in-transit is a platform/config fact) and Sheet B (operations fact). If unanswered, Q-29 also stays open.** |
| **Q-37** | *(New, from D-22.)* **Intercompany** transfer orders are **assumed out of scope** for v1 — confirm. | Intercompany TOs add subsidiary/costing complexity the WMS does not currently model. | Release scope | **Assumed out of scope;** confirm with the sponsor. | Sponsor | **Open — assumption-if-no-response: out of scope v1, review 2026-08-24** |
| **Q-40** | *(New, Sheet A / A1–A3 — developer platform facts, no safe default.)* **TO ledger-adapter transforms & receipt ordering.** (A1) Is `record.transform` from `transferorder` → `itemfulfillment` the correct server-side route, and what line/location/required-field constraints differ from an SO fulfilment? (A2) On the destination, is the Item Receipt transformed from the TO or from the Item Fulfilment, and does in-transit change that? (A3) Can an Item Receipt for a TO be created **before** its fulfilment posts, or must the queue guarantee fulfil-before-receive? | Unblocks the T-2.7 ledger adapter (transform target is currently *pending developer confirmation*) and **confirms or overturns F-30's deferral rule** — if NetSuite tolerates either order, the DEFERRED-and-retry logic is unnecessary. | T-2.7, Phase 5B, F-30 | **No safe default — do not assert platform behaviour.** Held until answered. | Developer | **Open — Sheet A; chase 2026-08-17** |
| **Q-41** | *(New, Sheet A / A4.)* Can a Suitelet return a **non-200 HTTP status** (e.g. 429 / 503) so the client distinguishes "server busy, retry" from "request rejected" — or does everything return 200 with the status in the body? | Determines whether F-29's busy-retry path is a clean status-code check or needs a body-inspection workaround; feeds T-3.1/T-3.2 client logic. | T-3.1, T-3.2, F-29 | **No safe default.** Held until answered. | Developer | **Open — Sheet A; chase 2026-08-17** |
| **Q-42** | *(New, Sheet A / A5.)* Roughly what do `record.create` and `submitFields` on a **custom record** cost in **governance units**? | Sets the F-29 batch-size config value — how many scan events fit in one 1,000-unit request with headroom. T-12.1 measures the real figure; this is the design estimate. | T-0.2, T-3.2, F-29, Q-34 | **No safe default;** measure. Held until answered. | Developer | **Open — Sheet A; chase 2026-08-17** |
| **Q-43** | *(New, Sheet A / A6.)* Can the Execute-As role be granted **create** on a custom record **without edit or delete**? | Confirms **AD-19** privilege separation is *enforceable* by role, not merely aspirational — the public ingest surface must append to the event queue and nothing else. | T-1.4, T-3.3, AD-19 | **No safe default.** Held until answered. | Developer | **Open — Sheet A; chase 2026-08-17** |
| **Q-44** | *(New, Sheet A / A8.)* Does **Map/Reduce** processing draw on the **same concurrency pool** as Suitelet/RESTlet traffic, or a **separate processor queue**? | Tells us whether the continuously-running committer competes with handheld responsiveness — a shared pool changes F-29's slot budget and the committer's scheduling. | T-0.2, T-4.1, F-29 | **No safe default.** Held until answered. | Developer | **Open — Sheet A; chase 2026-08-17** |
| ~~Q-45~~ | **RESOLVED 2026-08-11 (D-32) — customer returns (RMA) are IN scope.** RMA receipt joins PO, TO and WO receipts in the Phase 5B inbound path (new task T-5.11) and is one of the three serial entry points (D-29). Returned goods route to a RETURN/QUALITY bin pending review; fit-for-sale stock moves back via an Inventory Transfer (T-5.10). *(Earlier considered view, Q-05: defer to release 2 — overridden.)* | — | — | — | Sponsor | **Closed (D-32)** |
| **Q-46** | *(New, Sheet B / B8 — was part of Q-05.)* **Cycle counting.** The plan has a nightly reconciliation backstop but **no cycle-count workflow** (directed counts, variance approval, adjustment posting). Not in the source FRD. Is cycle counting expected in v1, a later phase, or handled by existing process? | Bin quantities drift (miscount, damage, unrecorded movement); reconciliation detects but does not direct a recount. | Release scope | **No recommendation** (current) — confirm expected in v1 / later / handled elsewhere. If elsewhere, record as an explicit exclusion. *Earlier considered view (Q-05): **defer both to release 2**, since the Phase 5B putaway engine makes them cheaper to add later — retained so the sponsor sees this was thought about once.* | Sponsor | **Open — no default; confirm or record exclusion** |
| **Q-47** | *(New, Sheet C / C4 — you / Todd.)* **Environment.** Which sandbox is the DEV target, who holds SDF deploy rights, and is there a **refresh schedule** that would wipe work in progress? | A sandbox refresh mid-build destroys deployed customisations and test data; SDF deploy rights gate T-0.5 onward. Account fact only Todd can get. | Phase 0 / Phase 1 | Confirm the sandbox, the authid owner and the refresh calendar before Phase 1 deploys anything. | Todd | **Open — Sheet C; Phase-1 gate** |
| **Q-48** | *(New, Sheet B / from D-24.)* **Is camera-based scanning (`getUserMedia`) required**, given the PWA must run on phones (D-24)? A hardware keyboard-wedge imager is the primary input on every surface, but a **phone operator may have no wedge hardware — in which case the camera is the only input path.** | Determines whether v1 must build a camera scan path for phone operators, or whether every operator surface has a hardware imager. Keyboard-wedge stays primary regardless. | Phase 3 | **No recommendation** — confirm whether phone operators will have wedge hardware; if not, camera is required, not optional. | Sponsor | **Open — dependency of D-24 phone support** |
| **Q-49** | *(New, Sheet C / from D-27, F-29.)* **What service tier is this account (Standard/Premium/Enterprise/Ultimate), and how many SuiteCloud Plus licences?** | **Sizes the entire F-29 concurrency design** (PF-01): base 5/15/20/20 +10 per SC+ licence. Sixty devices may contend for as few as 5 slots. Everything in F-29 sizes off this one number and it is unknown. | Phase 0 / Phase 3 | Read from the account. No safe default — the design is sized, not guessed. | Todd | **Open — Sheet C; blocks F-29 sizing** |
| **Q-50** | *(New, Sheet C / from D-27, PF-22.)* **Is the Enhanced Validations and Defaulting SuiteApp (bundle 213294) installed, and is "Prevent Negative Inventory" switched on?** | If yes, the WMS negative pre-check gains a platform backstop; if no, **the WMS check is the ONLY control** for plain items (invariant #19). | Phase 0 / Phase 4 | Read from the account (SANDBOX-PENDING). No safe default. | Todd | **Open — Sheet C** |
| **Q-51** | *(New, Sheet C / from D-27, PF-25.)* **Is Multi-Book accounting enabled?** | With Extended Accounting Period Close the shared `closed` flag is true only when closed in *all* books; if Multi-Book is on, the closed-period pre-check (invariant #16/F-23) must inspect book-specific status. | Phase 0 / Phase 4 | Read from the account. | Todd | **Open — Sheet C** |
| **Q-52** | *(New, Sheet C / from D-27, PF-19.)* **Is the account OneWorld with cross-subsidiary features?** | Decides whether **`PARTIAL_FULFILL_RCEIV_DISALLWD`** (cross-subsidiary only) applies and which subsidiary/location field names are in force for the ledger adapter (T-2.7) and TO receipt path. | Phase 0 / Phase 4 | Read from the account. | Todd | **Open — Sheet C** |
| **Q-53** | *(New, Sheet B / from D-27, D-33.)* **Which GL account offsets an inventory write-off, and is there a value threshold above which finance — not a warehouse supervisor — must approve?** | The committer's negative/adjustment and write-off (D-33) paths need the offset account; large write-offs may need a finance approval gate. NetSuite's **Default Inventory Count Account** preference may already answer the first half. | Phase 8 / write-off (T-5.12) | Confirm the offset account and any approval threshold; check the Default Inventory Count Account preference first. | Sponsor | **Open — Sheet B** |
| **Q-54** | *(New, from D-29 / §3.13b `SERIAL_UNKNOWN`.)* **The serial adopt path:** when a scanned serial exists in **NetSuite but has no WMS serial-state row** (stock that entered outside the WMS), how is it adopted? This is acute at **cutover and during parallel running**, when NetSuite holds serials the WMS has never seen. | Without an adopt path, every legitimately-pre-existing serial raises `SERIAL_UNKNOWN` and stalls picking. The WMS must **not** create rows on the fly (§3.13b), so adoption needs a controlled, audited flow. | Phase 5B / cutover (T-13.3) | Define an adopt flow (supervisor-authorised, reconciled against NetSuite) and a cutover seeding step; confirm parallel-running policy. | Sponsor / Todd | **Open — cutover-critical** |
